# Image placeholder

Put the headshot here as:

    public/reanna.jpg

Recommended: a portrait-oriented photo (roughly 4:5, e.g. 1000x1250px), well-lit, simple background.
The site will automatically crop/frame it. If the file is missing, the hero section shows a placeholder box instead of breaking.
